{
  "name": "aavegotchi-eth-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ aavegotchi-eth-subgraph",
    "create-local": "graph create --node http://localhost:8020/ aavegotchi-eth-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ aavegotchi-eth-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aavegotchi-eth-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}
